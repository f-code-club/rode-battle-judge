services:
  judge:
    image: ${REGISTRY}/judge:latest
    build:
      context: ../
      dockerfile: ./.docker/Dockerfile
      cache_from:
        - type=gha
      cache_to:
        - type=gha,mode=max
